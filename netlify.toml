[build]
  publish = "public"
  command = "npm run build"
  functions = "lambda"
[build.environment]
  NODE_VERSION = "16"
  YARN_VERSION = "1.22.19"
  YARN_FLAGS = "--no-ignore-optional"
[[plugins]]
package = "@netlify/plugin-gatsby"
